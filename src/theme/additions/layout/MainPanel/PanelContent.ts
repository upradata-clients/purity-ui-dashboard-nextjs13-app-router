import { defineStyleConfig } from '@chakra-ui/system';

export const PanelContent = defineStyleConfig({
    baseStyle: {
        // ms: 'auto',
        // me: 'auto',
        h: '100%',
        ps: '15px',
        pe: '15px',
    },
});

 