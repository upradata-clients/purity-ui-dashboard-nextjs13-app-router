import { defineStyleConfig } from '@chakra-ui/system';

export const linkStyles = defineStyleConfig({
    baseStyle: {
       /*  _hover: {
            textDecoration: 'none',
        },
        _focus: {
            boxShadow: 'none',
        }, */
    },
});
