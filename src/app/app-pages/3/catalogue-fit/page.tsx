'use client';

import { PageContent } from '@/components';
export default () => {
    // const textColor = useColorModeValue('gray.700', 'white');

    return (
        <PageContent>
                It is time to right something
        </PageContent>
    );
};
