import { NextResponse } from 'next/server';
import type { NextRequest } from 'next/server';

export function middleware(_request: NextRequest) {
    return NextResponse.redirect(new URL('/pages/dashboard'));
}


export const config = {
    matcher: '/',
};